import type { Habit } from "./habits";
import {
  addHabit,
  toggleHabitDone,
  deleteHabit,
  getHabitByName,
} from "./habits";

export function interaction_router(/*req: Request*/) {
	//if request is 
}